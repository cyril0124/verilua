# Set variables for verilua. Only VL_CFG_FILE is required, which is a path to a configuration file(in Lua).
# Verilua use the configuration file to setup the runtime environment.
export VL_CFG_FILE="$(pwd)/cfg.lua"