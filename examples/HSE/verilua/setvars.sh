# Set variables for verilua. Only VERILUA_CFG is required, which is a path to a configuration file(in Lua).
# Verilua use the configuration file to setup the runtime environment.
export VERILUA_CFG="$(pwd)/cfg.lua"